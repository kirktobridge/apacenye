ABOUT ME (design every explanation and interface decision around this):
I have a strong technical/STEM background — I'm generally comfortable with probability, statistics, optimization, and software engineering/computer science concepts. I work in Data Management at a large bank, working with funding and investment products (securities, repos, munis, derivatives, etc.) but I am more on the technical/data architecture/mapping/orchestration side than the product side. I have very little trading experience beyond casual exposure to Kalshi and stocks. I don't yet have strong intuition for concepts like implied probability vs. true probability, bid/ask spread, slippage, position sizing trade-offs, or drawdown risk. Treat this as a first-principles design constraint: wherever the system introduces a trading concept, ground it in simpler terms I already understand rather than assuming trading vocabulary or industry shorthand.

STACK COMFORT (enumerated so no stage has to guess): The one language I can fluently READ and REVIEW for correctness is **Python**. I am NOT fluent reviewing JavaScript/TypeScript, JVM languages (Java/Kotlin/Scala), or Go/Rust. Keep every money-touching component and all financial logic in Python, and keep any hand-written browser JavaScript to the absolute minimum. Any non-Python component must be justified as unavoidable and kept thin enough that I never have to review it to trust the money-touching logic.

RISK POSTURE (applies to every stage): This bootstrap is **PAPER-ONLY**. No stage may produce a system in which real capital is reachable. The live-order code path may be scaffolded, but live enablement must be hard-disabled behind a flag that cannot be turned on within this 5-stage bootstrap; enabling real capital is deferred to a separate future hardening session with its own acceptance gate. Wherever an instruction says "stop and ask me," interpret it as: if I am present, ask; if the session is unattended, record the blocking question in that stage's handoff "Open Decisions" with a proposed conservative default, and do NOT resolve it silently in any direction that affects money or safety.